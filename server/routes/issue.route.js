import { Router } from "express";

const router = Router();

// Placeholder routes - to be implemented
router.get("/", (req, res) => {
    res.json({ message: "Issue routes - coming soon" });
});

export default router;
